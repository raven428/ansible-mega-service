---
- name: Install groups create
  ansible.builtin.include_role:
    name: ansible-mega-var
    tasks_from: _group.yaml
  loop_control:
    loop_var: i
  loop: "{{ mesv_install_groups | default([]) }}"

- name: Install users create
  ansible.builtin.include_role:
    name: ansible-mega-var
    tasks_from: _user.yaml
  loop_control:
    loop_var: i
  loop: "{{ mesv_install_users | default([]) }}"

- name: Install dirs create
  ansible.builtin.include_role:
    name: ansible-mega-var
    tasks_from: _file.yaml
  loop_control:
    loop_var: i
  loop: "{{ mesv_install_dirs | default([]) }}"

- name: Install users keys
  ansible.builtin.include_role:
    name: ansible-mega-var
    tasks_from: _key.yaml
  loop_control:
    loop_var: i
  loop: "{{ mesv_install_key2user | default([]) }}"

# add ansible.builtin.apt_repository and ansible.builtin.apt_key
# by list if no current version or force set
# ansible/roles/external/tenable/agent-install/tasks/install.yaml
- name: Install packages install
  ansible.builtin.include_role:
    name: ansible-mega-var
    tasks_from: _apt.yaml
  loop_control:
    loop_var: i
  loop: "{{ mesv_install_pkgs | default([]) }}"
# remove repo and keys by list if was added

- name: Install templates upload
  ansible.builtin.include_role:
    name: ansible-mega-var
    tasks_from: _template.yaml
  loop_control:
    loop_var: i
  loop: "{{ mesv_install_templates | default([]) }}"

- name: Install files upload
  ansible.builtin.include_role:
    name: ansible-mega-var
    tasks_from: _copy.yaml
  loop_control:
    loop_var: i
  loop: "{{ mesv_install_files | default([]) }}"

- name: Install rsync tree
  ansible.builtin.include_role:
    name: ansible-mega-var
    tasks_from: _rsync.yaml
  loop_control:
    loop_var: i
  loop: "{{ mesv_install_rsync | default([]) }}"

- name: Install systemd units
  ansible.builtin.include_role:
    name: ansible-mega-var
    tasks_from: _systemd.yaml
  loop_control:
    loop_var: i
  loop: "{{ mesv_install_service | default([]) | selectattr('systemd', 'defined') |
    map(attribute='systemd') | list }}"

- name: Install download files
  ansible.builtin.include_role:
    name: ansible-mega-var
    tasks_from: _download.yaml
  loop_control:
    loop_var: i
  loop: "{{ mesv_install_downloads | default([]) }}"

- name: Install launch shell
  ansible.builtin.include_role:
    name: ansible-mega-var
    tasks_from: _shell.yaml
  loop_control:
    loop_var: i
  loop: "{{ mesv_install_shell | default([]) }}"
